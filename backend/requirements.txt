# ── Core Framework ──
fastapi>=0.111.0
uvicorn>=0.29.0
pydantic>=2.7.1
pydantic-settings>=2.2.1

# ── Database ──
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
asyncpg>=0.29.0

# ── AI / LLM (only what's actually imported) ──
langchain>=0.2.1
langchain-openai>=0.1.8
openai>=1.30.1

# ── Astrology ──
skyfield>=1.46

# ── Chinese Calendar / BaZi ──
lunar-python>=1.3.12

# ── Vision: face & palm analysis ──
mediapipe>=0.10.14
opencv-python-headless>=4.9.0

# ── Utilities (lightweight only) ──
httpx>=0.27.0
requests>=2.31.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
tenacity>=8.3.0
mangum>=0.17.0
email-validator>=2.0.0
numpy>=1.24.0

# ── Distributed state (Redis) ──
redis[hiredis]>=5.0.0

# ── Security: file upload validation ──
python-magic>=0.4.27

# ── Security: XML parsing ──
defusedxml>=0.7.1
